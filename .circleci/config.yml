version: 2.1
jobs:
  name: smoke_test
  docker:
   - image: alpine:latest
  steps:
    - checkout
    - run:
      name: check_env
      commands: |
       if curl -s --head "https://google.com" 
       then
       echo "It worked!"
       else
       echo "It failed"
       fi
workflows:
 yes:
  jobs:
   - smoke_test
 #success
   
